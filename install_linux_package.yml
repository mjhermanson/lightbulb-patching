---
- name: Install Package on Linux host(s)
  gather_facts: "no"
  hosts:
    - "{{ linux_package_host }}"
  tasks:
    - name: Install package
      package:
        name: "{{ linux_package_name }}"
        state: absent

