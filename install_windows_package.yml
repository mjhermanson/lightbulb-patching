---
- name: Manage Package on Windows(s)
  gather_facts: "yes"
  vars_files:
    - vars/windows.yml
  hosts:
    - "{{ windows_package_host }}"
  tasks:
    - name: Manage package
      win_chocolatey:
        name: "{{ windows_package_name }}"
        state: "{{ windows_package_state }}"
